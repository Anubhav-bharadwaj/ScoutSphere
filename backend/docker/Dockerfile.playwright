FROM python:3.11-slim

WORKDIR /app

# Install Playwright and its dependencies
RUN pip install --no-cache-dir playwright
RUN playwright install --with-deps chromium

COPY . /app

CMD ["python", "scripts/dom_to_markdown.py"]
